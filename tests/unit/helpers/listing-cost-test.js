import { listingCost } from 'craigslist/helpers/listing-cost';
import { module, test } from 'qunit';

module('Unit | Helper | listing cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = listingCost([42]);
  assert.ok(result);
});
